import { CheckattendencePipe } from './checkattendence.pipe';

describe('CheckattendencePipe', () => {
  it('create an instance', () => {
    const pipe = new CheckattendencePipe();
    expect(pipe).toBeTruthy();
  });
});
