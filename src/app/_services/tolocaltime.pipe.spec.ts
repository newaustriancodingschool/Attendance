import { TolocaltimePipe } from './tolocaltime.pipe';

describe('TolocaltimePipe', () => {
  it('create an instance', () => {
    const pipe = new TolocaltimePipe();
    expect(pipe).toBeTruthy();
  });
});
