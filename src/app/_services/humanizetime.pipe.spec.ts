import { HumanizetimePipe } from './humanizetime.pipe';

describe('HumanizetimePipe', () => {
  it('create an instance', () => {
    const pipe = new HumanizetimePipe();
    expect(pipe).toBeTruthy();
  });
});
